<div class="container pt-3">
  <div class="row">
    <div class="col-10 mx-auto">
      <table class="table">
        <thead >
          <tr>
            <th class="text-center">コメント一覧</th>
          </tr>
        </thead>
        <% @post.each do |comment| %>
        <tbody>

          <td class="col-4"><%= comment.created_at.strftime('%Y/%m/%d %H:%M') %></td>
          <td class="col-4"><%= link_to post_path(comment.post.id) do %><%= comment.comment %><% end %></td>
          <td class="col-4"><%= comment.customer.name %></td>
        </tbody>
        <% end %>
      </table>
    </div>
  </div>
</div>