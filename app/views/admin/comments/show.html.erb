<div class="container pt-3">
  <div class="row mx-auto">
    <div class="col-9 text-center">

        <p>コメント</p>
        <%= @comment.comment %></br>
        <%= @comment.customer.name %></br>
        <%= link_to '投稿詳細' admin_post_path(@comment.post.id), class:"btn btn-success mr-5" %>
        <%= link_to '削除' admin_post_comment_path(@comment.post.id,@comment.id), method: :destroy, "data-confirm" => "本当に削除しますか？" , class:"btn btn-success mr-5" %>


    </div>
  </div>
</div>
